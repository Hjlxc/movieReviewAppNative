import 'react-native';
import React from 'react';
import {render, cleanup, fireEvent} from '@testing-library/react-native';
import renderer from 'react-test-renderer';

// import SearchBox from '../../container/SearchBox';

test('test', () => {
  //   renderer.create(<SearchBox />);
});
